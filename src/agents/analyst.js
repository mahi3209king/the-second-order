export function analystOpinion(news) {
  return `
This isn’t just about "${news.title}".

The real impact is how this quietly shifts incentives in the tech ecosystem.
Short term reactions will miss the long-term consequence.

This is where second-order effects start to matter.
`.trim();
}
